spring:
  application:
    name: organization-service
  cloud:
    config:
      uri: http://localhost:8071
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:10000/realms/ostock
